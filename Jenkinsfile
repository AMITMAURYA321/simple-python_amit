1. create a folder
2. create 2 files and write shell scripts in both the files :

-- file 1 : Write a shell script to print product of numbers from 1 to 20.
-- file 2 : write a shell script to see whether a variable is empty or not ?

3. Create 2 files in a different branch and write the shell scripts below :

-- bfile 1 : Write a shell script to see whether the two given strings are equal or not ?
-- bfile 2 : write a shell script to print even and odd numbers separately from 1 to 30 

4. Create a pipeline job in jenkins and run the 2 branches in a single job.

